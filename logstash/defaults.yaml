# -*- coding: utf-8 -*-
# vim: ft=yaml
logstash:
  pkg: "logstash"
  svc: "logstash"
  pkgstate: "latest"
  indent: 4
  repoversion: "2.3"
  configfile: "/etc/logstash/logstash.conf"
  service:
    name: logstash
